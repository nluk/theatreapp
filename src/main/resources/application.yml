spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password: password
    platform: org.hibernate.dialect.H2Dialect
    url: jdbc:h2:file:/home/nluk/Studia/theatre/data
  jpa:
    hibernate:
      ddl-auto: create